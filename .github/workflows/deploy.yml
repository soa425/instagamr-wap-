name: Deploy to S3 and Invalidate CloudFront

on: 
  push:
    branches: 
      - main

jobs: 
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: github Repository 파일불러오기
        uses: actions/checkout@v4

        - name: Repository 파일불러오기
           run: ls 
           